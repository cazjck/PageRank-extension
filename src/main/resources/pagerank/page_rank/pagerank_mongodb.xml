<?xml version="1.0" encoding="UTF-8"?>
<?xml-stylesheet type="text/xsl" href="../../../../../rapidminerreferencemanual/documentation2html.xsl"?>
<p1:documents xmlns:p1="http://rapid-i.com/schemas/documentation/reference/1.0"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://rapid-i.com/schemas/documentation/reference/1.0/documentation.xsd">
	<operator key="operator.pagerank:pagerank_mongodb_read" locale="en" version="6.0.003">
		<title>PageRank on MongoDB</title>
		<synopsis>Execute algorithm PageRank using MapReduce on MongoDB .</synopsis>
		<text>
			<paragraph>
				This operator using MapReduce on MongoDB.
				The query criteria, the query projection and sorting criteria can be specified via JSON/BSON documents.
			</paragraph>
		</text>
		<inputPorts>
			<port name="criteria" type="com.rapidminer.operator.text.Document">
				The query criteria which can be used to select only specific documents (a JSON/BSON document).
				Alternatively, you can specify this document via the criteria parameter.
				Note that the parameter is only visible if no document is connected to this input port.
			</port>
			<port name="projection" type="com.rapidminer.operator.text.Document">
				The query projection which can be used to include/exclude specific fields from the results (a JSON/BSON document).
				Alternatively, you can specify this document via the projection parameter.
				Note that the parameter is only visible if no document is connected to this input port.
			</port>
			<port name="sorting criteria" type="com.rapidminer.operator.text.Document">
				The sorting criteria which can be used to sort the returned documents in a specific order (a JSON/BSON document).
				Alternatively, you can specify this document via the sort document and sorting criteria parameters.
				Note that the parameter is only visible if no document is connected to this input port.
			</port>
		</inputPorts>
		<outputPorts>
			<port name="collection" type="com.rapidminer.operator.IOObjectCollection">
				The documents retrieved from the MongoDB collection.
			</port>
		</outputPorts>
		<parameters>
			<parameter key="mongodb_pagerank_instance" type="configurable">
				The MongoDB instance to be used for storing the documents.
			</parameter>
			<parameter key="collection" type="string">
				The MongoDB collection in which the documents are stored.
			</parameter>
			<parameter key="criteria" type="String">
				The query criteria which can be used to select only specific documents (a JSON/BSON document).
				Alternatively, you can specify this document via the criteria input port.
			</parameter>
			<parameter key="projection" type="String">
				The query projection which can be used to include/exclude specific fields from the results (a JSON/BSON document).
				Alternatively, you can specify this document via the projection input port.
			</parameter>
			<parameter key="sort_documents" type="boolean">
				If checked, a sorting criteria document can be specified to sort the query results.
				Alternatively, you can enable this behavior by connection a sorting criteria document to the sorting input port.
			</parameter>
			<parameter key="sorting_criteria" type="String">
				The sorting criteria which can be used to sort the returned documents in a specific order (a JSON/BSON document).
				Alternatively, you can specify this document via the sorting input port.
			</parameter>
			<parameter key="limit_results" type="boolean">
				Whether the number of results should be limited.
			</parameter>
			<parameter key="limit" type="integer">
				The number of documents to be queried.
			</parameter>
			<parameter key="skip" type="integer">
				The number of documents to be skipped.
			</parameter>
		</parameters>
	</operator>
</p1:documents>
